&{cowichan_pony}

# first we will start by implementing a serial version of product

```pony
use collections
use "path:~/SciPony"
use "lib:gsl"

actor Main
       new create(env:Env)=>
           let
```

